\usepackage{csquotes,mathptmx,emptypage,setspace,pdflscape,multirow,dcolumn,graphicx,tikz,pdfpages}
% custom quotes, Times font, emptypage custom, custom spacing, landscape, table multirow, define columns,graphicneeds,togeneratecooldiagramswithsyntax,includeexternalpdfpages

\usepackage[nameinlink,noabbrev,capitalise]{cleveref}
% Remove parenthesis around equation numbers
% NB: This option demands either LuaLaTeX or XeLaTeX
\creflabelformat{equation}{#2\textup{#1}#3}

% Introduce some space between ``Figure/Table 1'' and its caption
\DeclareCaptionLabelSeparator{apalabelsep}{\\[0.5\baselineskip]}

% Align numbers along their the decimal points
\newcolumntype{d}[1]{D{.}{.}{#1}}

% Configure tikz
\usetikzlibrary{automata,shapes,arrows,calc,positioning,decorations.pathmorphing}

% Turn off fancy header.
\pagestyle{plain}

% Prevents LaTeX stretching paragraph gaps
\raggedbottom

% APA7 Rule 2.24: Indent the first line of every paragraph 0.5 in
\setlength\parindent{0.5in}

\newcommand{\appindent}{\phantom{1}\par\vspace{-0.5\baselineskip}} %Somehow no indentation in appendix at first, so create phantom paragraph as quickfix and leave some extra whitespace after appendix title}

% APA7 Rule 2.24 Paragraph Indentation: Block quotation should be indented from the left margin 0.5 in.
%Would need to customize using csquotes: can also just omit for simplicity :)

% Automate the table/figure production process
\newcommand{\MAEtable}[5]{
    \ifx  l#5 \begin{landscape}\fi
    \begin{table}[htbp]
        \begin{threeparttable}[htbp]
            \caption{#2}
            \label{#1}
                #3
            \begin{tablenotes}
                \linespread{1.75}\selectfont
                \textit{Note.} #4
            \end{tablenotes}
        \end{threeparttable}
    \end{table}
    \ifx  l#5 \end{landscape}\fi
}

\newcommand{\MAEfig}[5]{
    \ifx  l#5 \begin{landscape}\fi
    \begin{figure}[htbp]
        \caption{#2}
        \label{#1}
            \includegraphics[width=\linewidth]{#3}
%        \begin{tablenotes}
            \linespread{1.75}\selectfont
            \textit{Note.} #4
%        \end{tablenotes}
    \end{figure}
    \ifx  l#5 \end{landscape}\fi
}


\title{\titlet}
\shorttitle{}
\author{\authort}
% Determine whether we are in spring or autumn semester
\ifthenelse{\the\month<7}
    {\newcommand{\semester}{Spring~\the\year}}
    {\newcommand{\semester}{Autumn~\the\year}}
\date{\semester}

% *.bib file with your references:
% Export manually from Zotero OR make your Zotero group library public and create a new file in overleaf
% Choose from an external url and fill the link after customizing it to your group library: https://api.zotero.org/groups/INSERT_GROUPID/items/top?format=bibtex&style=numeric&limit=100.
% Name the file Main.bib (or another name)
% This will keep your bib-file that stores the references in sync with Zotero.
\usepackage[backend=biber,style=apa]{biblatex}
\addbibresource{./Bibliography/Main.bib}

% Subdirectory to store the figures
\graphicspath{{Figures/}}

%%%%%%%%%%CUSTOMIZATION OPTIONS
% I have been typing ``with respect to'' too many times so here is a shortcut:
%Use as \wrt in syntax but get it fully spelled out in text
\newcommand{\wrt}{with respect to\ }
% Absolute value as value in between vertical bars: use as \abs{5} to get |5|
\newcommand{\abs}[1]{\left|#1\right|}
